import React, {Fragment} from 'react';

const Counter = (props) => {
    return(
        <Fragment>
            <div>
                Counter
            </div>
        </Fragment>
    )      
}

export default Counter;